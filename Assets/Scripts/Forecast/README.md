# Forecast

Moteur de prévision (`ForecastCalculator`, `ForecastOccurrenceGenerator`, `VariableAmountEstimator`, `ReconciliationSuggester`).

Pur C#, sans dépendance à `UnityEngine` — testable en Edit Mode. Voir `docs/06-Moteur_de_prevision.md`.
